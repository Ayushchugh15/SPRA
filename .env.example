# SPRA - Environment Configuration Template
# Copy this file to .env and fill in your values
# IMPORTANT: Do NOT commit .env to git - it contains sensitive data!

# ==================== REQUIRED ====================

# Flask Environment
FLASK_ENV=production

# Secret Key - Generate with: python -c "import secrets; print(secrets.token_hex(32))"
# CRITICAL: Use a different key for each environment!
SECRET_KEY=your-secure-random-key-here-minimum-32-characters

# Database - Use PostgreSQL for production!
# SQLite: sqlite:///data/spra.db
# PostgreSQL: postgresql://username:password@host:5432/database
DATABASE_URL=postgresql://spra_user:spra_password_123@localhost:5432/spra

# ==================== RECOMMENDED ====================

# Debug Mode (ALWAYS false in production)
DEBUG=false

# CORS - Restrict API access to your company domain(s)
# Examples:
#   CORS_ORIGINS=https://yourdomain.com
#   CORS_ORIGINS=https://app.yourdomain.com,https://admin.yourdomain.com
CORS_ORIGINS=https://yourdomain.com

# Server Configuration
HOST=0.0.0.0
PORT=5000

# Production Workers (for Gunicorn/Waitress)
WORKERS=4

# ==================== OPTIONAL ====================

# Custom data storage location
# SPRA_DATA_DIR=/var/lib/spra
# SPRA_DATA_DIR=D:\SPRA_Data

# Email Configuration (for future notifications)
# MAIL_SERVER=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USERNAME=your-email@gmail.com
# MAIL_PASSWORD=your-app-password
# MAIL_USE_TLS=true

# Backup Configuration
# BACKUP_PATH=/var/backups/spra
# BACKUP_SCHEDULE=daily
